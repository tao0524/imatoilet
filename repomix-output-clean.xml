This file is a merged representation of a subset of the codebase, containing files not matching ignore patterns, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching these patterns are excluded: **/node_modules/**, **/target/**, **/dist/**, **/.git/**, **/.metadata/**, **/Servers/**, **/.settings/**, **/.classpath, **/.project, **/.cache/**, **/.next/**, **/coverage/**, **/*.map, **/*.min.js, **/*.log, repomix-output*.xml, restore.js
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
backend/backend/.gitattributes
backend/backend/.gitignore
backend/backend/.mvn/wrapper/maven-wrapper.properties
backend/backend/mvnw
backend/backend/mvnw.cmd
backend/backend/pom.xml
backend/backend/src/main/java/com/imatoilet/backend/BackendApplication.java
backend/backend/src/main/java/com/imatoilet/backend/config/WebConfig.java
backend/backend/src/main/java/com/imatoilet/backend/DataLoader.java
backend/backend/src/main/java/com/imatoilet/backend/Equipment.java
backend/backend/src/main/java/com/imatoilet/backend/EquipmentRepository.java
backend/backend/src/main/java/com/imatoilet/backend/EquipmentType.java
backend/backend/src/main/java/com/imatoilet/backend/exception/ErrorResponse.java
backend/backend/src/main/java/com/imatoilet/backend/exception/GlobalExceptionHandler.java
backend/backend/src/main/java/com/imatoilet/backend/exception/ResourceNotFoundException.java
backend/backend/src/main/java/com/imatoilet/backend/HelloController.java
backend/backend/src/main/java/com/imatoilet/backend/Toilet.java
backend/backend/src/main/java/com/imatoilet/backend/ToiletApiController.java
backend/backend/src/main/java/com/imatoilet/backend/ToiletRepository.java
backend/backend/src/main/java/com/imatoilet/backend/ToiletService.java
backend/backend/src/main/resources/application.properties
backend/backend/src/main/resources/db/migration/V2__add_equipment_table.sql
backend/backend/src/main/resources/db/migration/V3__add_search_indexes.sql
backend/backend/src/main/resources/db/migration/V4__insert_sample_data.sql
backend/backend/src/main/resources/db/migration/V5__add_image_column.sql
backend/backend/src/main/resources/templates/toilet-form.html
backend/backend/src/main/resources/templates/toilet-list.html
backend/backend/src/test/java/com/imatoilet/backend/BackendApplicationTests.java
backend/backend/src/test/java/com/imatoilet/backend/ToiletApiControllerTest.java
toilet-frontend/.env.example
toilet-frontend/.gitignore
toilet-frontend/eslint.config.js
toilet-frontend/index.html
toilet-frontend/package.json
toilet-frontend/public/images/hero.png
toilet-frontend/public/vite.svg
toilet-frontend/README.md
toilet-frontend/src/App.css
toilet-frontend/src/App.jsx
toilet-frontend/src/assets/react.svg
toilet-frontend/src/base.css
toilet-frontend/src/components.css
toilet-frontend/src/components/ErrorBoundary.jsx
toilet-frontend/src/components/Header.jsx
toilet-frontend/src/components/Header.test.jsx
toilet-frontend/src/components/SafeGoogleMap.jsx
toilet-frontend/src/components/ToiletCard.jsx
toilet-frontend/src/components/ToiletCard.test.jsx
toilet-frontend/src/config/api.js
toilet-frontend/src/hooks/useToiletSearch.js
toilet-frontend/src/index.css
toilet-frontend/src/main.jsx
toilet-frontend/src/mocks/handlers.js
toilet-frontend/src/mocks/server.js
toilet-frontend/src/pages/Conditions.jsx
toilet-frontend/src/pages/Detail.jsx
toilet-frontend/src/pages/Detail.test.jsx
toilet-frontend/src/pages/Edit.jsx
toilet-frontend/src/pages/Favorites.css
toilet-frontend/src/pages/Favorites.jsx
toilet-frontend/src/pages/Home.jsx
toilet-frontend/src/pages/Home.test.jsx
toilet-frontend/src/pages/Register.css
toilet-frontend/src/pages/Register.jsx
toilet-frontend/src/pages/Search/FilterPanel.jsx
toilet-frontend/src/pages/Search/index.jsx
toilet-frontend/src/pages/Search/ListPanel.jsx
toilet-frontend/src/pages/Search/MapPanel.jsx
toilet-frontend/src/pages/Search/PlaceSearch.jsx
toilet-frontend/src/search.css
toilet-frontend/src/setupTests.js
toilet-frontend/src/utils.js
toilet-frontend/src/utils.test.js
toilet-frontend/vite.config.js
toilet-frontend/vitest.config.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="backend/backend/.gitattributes">
/mvnw text eol=lf
*.cmd text eol=crlf
</file>

<file path="backend/backend/.gitignore">
HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/
</file>

<file path="backend/backend/.mvn/wrapper/maven-wrapper.properties">
wrapperVersion=3.3.4
distributionType=only-script
distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.12/apache-maven-3.9.12-bin.zip
</file>

<file path="backend/backend/mvnw">
#!/bin/sh
# ----------------------------------------------------------------------------
# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Apache Maven Wrapper startup batch script, version 3.3.4
#
# Optional ENV vars
# -----------------
#   JAVA_HOME - location of a JDK home dir, required when download maven via java source
#   MVNW_REPOURL - repo url base for downloading maven distribution
#   MVNW_USERNAME/MVNW_PASSWORD - user and password for downloading maven
#   MVNW_VERBOSE - true: enable verbose log; debug: trace the mvnw script; others: silence the output
# ----------------------------------------------------------------------------

set -euf
[ "${MVNW_VERBOSE-}" != debug ] || set -x

# OS specific support.
native_path() { printf %s\\n "$1"; }
case "$(uname)" in
CYGWIN* | MINGW*)
  [ -z "${JAVA_HOME-}" ] || JAVA_HOME="$(cygpath --unix "$JAVA_HOME")"
  native_path() { cygpath --path --windows "$1"; }
  ;;
esac

# set JAVACMD and JAVACCMD
set_java_home() {
  # For Cygwin and MinGW, ensure paths are in Unix format before anything is touched
  if [ -n "${JAVA_HOME-}" ]; then
    if [ -x "$JAVA_HOME/jre/sh/java" ]; then
      # IBM's JDK on AIX uses strange locations for the executables
      JAVACMD="$JAVA_HOME/jre/sh/java"
      JAVACCMD="$JAVA_HOME/jre/sh/javac"
    else
      JAVACMD="$JAVA_HOME/bin/java"
      JAVACCMD="$JAVA_HOME/bin/javac"

      if [ ! -x "$JAVACMD" ] || [ ! -x "$JAVACCMD" ]; then
        echo "The JAVA_HOME environment variable is not defined correctly, so mvnw cannot run." >&2
        echo "JAVA_HOME is set to \"$JAVA_HOME\", but \"\$JAVA_HOME/bin/java\" or \"\$JAVA_HOME/bin/javac\" does not exist." >&2
        return 1
      fi
    fi
  else
    JAVACMD="$(
      'set' +e
      'unset' -f command 2>/dev/null
      'command' -v java
    )" || :
    JAVACCMD="$(
      'set' +e
      'unset' -f command 2>/dev/null
      'command' -v javac
    )" || :

    if [ ! -x "${JAVACMD-}" ] || [ ! -x "${JAVACCMD-}" ]; then
      echo "The java/javac command does not exist in PATH nor is JAVA_HOME set, so mvnw cannot run." >&2
      return 1
    fi
  fi
}

# hash string like Java String::hashCode
hash_string() {
  str="${1:-}" h=0
  while [ -n "$str" ]; do
    char="${str%"${str#?}"}"
    h=$(((h * 31 + $(LC_CTYPE=C printf %d "'$char")) % 4294967296))
    str="${str#?}"
  done
  printf %x\\n $h
}

verbose() { :; }
[ "${MVNW_VERBOSE-}" != true ] || verbose() { printf %s\\n "${1-}"; }

die() {
  printf %s\\n "$1" >&2
  exit 1
}

trim() {
  # MWRAPPER-139:
  #   Trims trailing and leading whitespace, carriage returns, tabs, and linefeeds.
  #   Needed for removing poorly interpreted newline sequences when running in more
  #   exotic environments such as mingw bash on Windows.
  printf "%s" "${1}" | tr -d '[:space:]'
}

scriptDir="$(dirname "$0")"
scriptName="$(basename "$0")"

# parse distributionUrl and optional distributionSha256Sum, requires .mvn/wrapper/maven-wrapper.properties
while IFS="=" read -r key value; do
  case "${key-}" in
  distributionUrl) distributionUrl=$(trim "${value-}") ;;
  distributionSha256Sum) distributionSha256Sum=$(trim "${value-}") ;;
  esac
done <"$scriptDir/.mvn/wrapper/maven-wrapper.properties"
[ -n "${distributionUrl-}" ] || die "cannot read distributionUrl property in $scriptDir/.mvn/wrapper/maven-wrapper.properties"

case "${distributionUrl##*/}" in
maven-mvnd-*bin.*)
  MVN_CMD=mvnd.sh _MVNW_REPO_PATTERN=/maven/mvnd/
  case "${PROCESSOR_ARCHITECTURE-}${PROCESSOR_ARCHITEW6432-}:$(uname -a)" in
  *AMD64:CYGWIN* | *AMD64:MINGW*) distributionPlatform=windows-amd64 ;;
  :Darwin*x86_64) distributionPlatform=darwin-amd64 ;;
  :Darwin*arm64) distributionPlatform=darwin-aarch64 ;;
  :Linux*x86_64*) distributionPlatform=linux-amd64 ;;
  *)
    echo "Cannot detect native platform for mvnd on $(uname)-$(uname -m), use pure java version" >&2
    distributionPlatform=linux-amd64
    ;;
  esac
  distributionUrl="${distributionUrl%-bin.*}-$distributionPlatform.zip"
  ;;
maven-mvnd-*) MVN_CMD=mvnd.sh _MVNW_REPO_PATTERN=/maven/mvnd/ ;;
*) MVN_CMD="mvn${scriptName#mvnw}" _MVNW_REPO_PATTERN=/org/apache/maven/ ;;
esac

# apply MVNW_REPOURL and calculate MAVEN_HOME
# maven home pattern: ~/.m2/wrapper/dists/{apache-maven-<version>,maven-mvnd-<version>-<platform>}/<hash>
[ -z "${MVNW_REPOURL-}" ] || distributionUrl="$MVNW_REPOURL$_MVNW_REPO_PATTERN${distributionUrl#*"$_MVNW_REPO_PATTERN"}"
distributionUrlName="${distributionUrl##*/}"
distributionUrlNameMain="${distributionUrlName%.*}"
distributionUrlNameMain="${distributionUrlNameMain%-bin}"
MAVEN_USER_HOME="${MAVEN_USER_HOME:-${HOME}/.m2}"
MAVEN_HOME="${MAVEN_USER_HOME}/wrapper/dists/${distributionUrlNameMain-}/$(hash_string "$distributionUrl")"

exec_maven() {
  unset MVNW_VERBOSE MVNW_USERNAME MVNW_PASSWORD MVNW_REPOURL || :
  exec "$MAVEN_HOME/bin/$MVN_CMD" "$@" || die "cannot exec $MAVEN_HOME/bin/$MVN_CMD"
}

if [ -d "$MAVEN_HOME" ]; then
  verbose "found existing MAVEN_HOME at $MAVEN_HOME"
  exec_maven "$@"
fi

case "${distributionUrl-}" in
*?-bin.zip | *?maven-mvnd-?*-?*.zip) ;;
*) die "distributionUrl is not valid, must match *-bin.zip or maven-mvnd-*.zip, but found '${distributionUrl-}'" ;;
esac

# prepare tmp dir
if TMP_DOWNLOAD_DIR="$(mktemp -d)" && [ -d "$TMP_DOWNLOAD_DIR" ]; then
  clean() { rm -rf -- "$TMP_DOWNLOAD_DIR"; }
  trap clean HUP INT TERM EXIT
else
  die "cannot create temp dir"
fi

mkdir -p -- "${MAVEN_HOME%/*}"

# Download and Install Apache Maven
verbose "Couldn't find MAVEN_HOME, downloading and installing it ..."
verbose "Downloading from: $distributionUrl"
verbose "Downloading to: $TMP_DOWNLOAD_DIR/$distributionUrlName"

# select .zip or .tar.gz
if ! command -v unzip >/dev/null; then
  distributionUrl="${distributionUrl%.zip}.tar.gz"
  distributionUrlName="${distributionUrl##*/}"
fi

# verbose opt
__MVNW_QUIET_WGET=--quiet __MVNW_QUIET_CURL=--silent __MVNW_QUIET_UNZIP=-q __MVNW_QUIET_TAR=''
[ "${MVNW_VERBOSE-}" != true ] || __MVNW_QUIET_WGET='' __MVNW_QUIET_CURL='' __MVNW_QUIET_UNZIP='' __MVNW_QUIET_TAR=v

# normalize http auth
case "${MVNW_PASSWORD:+has-password}" in
'') MVNW_USERNAME='' MVNW_PASSWORD='' ;;
has-password) [ -n "${MVNW_USERNAME-}" ] || MVNW_USERNAME='' MVNW_PASSWORD='' ;;
esac

if [ -z "${MVNW_USERNAME-}" ] && command -v wget >/dev/null; then
  verbose "Found wget ... using wget"
  wget ${__MVNW_QUIET_WGET:+"$__MVNW_QUIET_WGET"} "$distributionUrl" -O "$TMP_DOWNLOAD_DIR/$distributionUrlName" || die "wget: Failed to fetch $distributionUrl"
elif [ -z "${MVNW_USERNAME-}" ] && command -v curl >/dev/null; then
  verbose "Found curl ... using curl"
  curl ${__MVNW_QUIET_CURL:+"$__MVNW_QUIET_CURL"} -f -L -o "$TMP_DOWNLOAD_DIR/$distributionUrlName" "$distributionUrl" || die "curl: Failed to fetch $distributionUrl"
elif set_java_home; then
  verbose "Falling back to use Java to download"
  javaSource="$TMP_DOWNLOAD_DIR/Downloader.java"
  targetZip="$TMP_DOWNLOAD_DIR/$distributionUrlName"
  cat >"$javaSource" <<-END
	public class Downloader extends java.net.Authenticator
	{
	  protected java.net.PasswordAuthentication getPasswordAuthentication()
	  {
	    return new java.net.PasswordAuthentication( System.getenv( "MVNW_USERNAME" ), System.getenv( "MVNW_PASSWORD" ).toCharArray() );
	  }
	  public static void main( String[] args ) throws Exception
	  {
	    setDefault( new Downloader() );
	    java.nio.file.Files.copy( java.net.URI.create( args[0] ).toURL().openStream(), java.nio.file.Paths.get( args[1] ).toAbsolutePath().normalize() );
	  }
	}
	END
  # For Cygwin/MinGW, switch paths to Windows format before running javac and java
  verbose " - Compiling Downloader.java ..."
  "$(native_path "$JAVACCMD")" "$(native_path "$javaSource")" || die "Failed to compile Downloader.java"
  verbose " - Running Downloader.java ..."
  "$(native_path "$JAVACMD")" -cp "$(native_path "$TMP_DOWNLOAD_DIR")" Downloader "$distributionUrl" "$(native_path "$targetZip")"
fi

# If specified, validate the SHA-256 sum of the Maven distribution zip file
if [ -n "${distributionSha256Sum-}" ]; then
  distributionSha256Result=false
  if [ "$MVN_CMD" = mvnd.sh ]; then
    echo "Checksum validation is not supported for maven-mvnd." >&2
    echo "Please disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties." >&2
    exit 1
  elif command -v sha256sum >/dev/null; then
    if echo "$distributionSha256Sum  $TMP_DOWNLOAD_DIR/$distributionUrlName" | sha256sum -c - >/dev/null 2>&1; then
      distributionSha256Result=true
    fi
  elif command -v shasum >/dev/null; then
    if echo "$distributionSha256Sum  $TMP_DOWNLOAD_DIR/$distributionUrlName" | shasum -a 256 -c >/dev/null 2>&1; then
      distributionSha256Result=true
    fi
  else
    echo "Checksum validation was requested but neither 'sha256sum' or 'shasum' are available." >&2
    echo "Please install either command, or disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties." >&2
    exit 1
  fi
  if [ $distributionSha256Result = false ]; then
    echo "Error: Failed to validate Maven distribution SHA-256, your Maven distribution might be compromised." >&2
    echo "If you updated your Maven version, you need to update the specified distributionSha256Sum property." >&2
    exit 1
  fi
fi

# unzip and move
if command -v unzip >/dev/null; then
  unzip ${__MVNW_QUIET_UNZIP:+"$__MVNW_QUIET_UNZIP"} "$TMP_DOWNLOAD_DIR/$distributionUrlName" -d "$TMP_DOWNLOAD_DIR" || die "failed to unzip"
else
  tar xzf${__MVNW_QUIET_TAR:+"$__MVNW_QUIET_TAR"} "$TMP_DOWNLOAD_DIR/$distributionUrlName" -C "$TMP_DOWNLOAD_DIR" || die "failed to untar"
fi

# Find the actual extracted directory name (handles snapshots where filename != directory name)
actualDistributionDir=""

# First try the expected directory name (for regular distributions)
if [ -d "$TMP_DOWNLOAD_DIR/$distributionUrlNameMain" ]; then
  if [ -f "$TMP_DOWNLOAD_DIR/$distributionUrlNameMain/bin/$MVN_CMD" ]; then
    actualDistributionDir="$distributionUrlNameMain"
  fi
fi

# If not found, search for any directory with the Maven executable (for snapshots)
if [ -z "$actualDistributionDir" ]; then
  # enable globbing to iterate over items
  set +f
  for dir in "$TMP_DOWNLOAD_DIR"/*; do
    if [ -d "$dir" ]; then
      if [ -f "$dir/bin/$MVN_CMD" ]; then
        actualDistributionDir="$(basename "$dir")"
        break
      fi
    fi
  done
  set -f
fi

if [ -z "$actualDistributionDir" ]; then
  verbose "Contents of $TMP_DOWNLOAD_DIR:"
  verbose "$(ls -la "$TMP_DOWNLOAD_DIR")"
  die "Could not find Maven distribution directory in extracted archive"
fi

verbose "Found extracted Maven distribution directory: $actualDistributionDir"
printf %s\\n "$distributionUrl" >"$TMP_DOWNLOAD_DIR/$actualDistributionDir/mvnw.url"
mv -- "$TMP_DOWNLOAD_DIR/$actualDistributionDir" "$MAVEN_HOME" || [ -d "$MAVEN_HOME" ] || die "fail to move MAVEN_HOME"

clean || :
exec_maven "$@"
</file>

<file path="backend/backend/mvnw.cmd">
<# : batch portion
@REM ----------------------------------------------------------------------------
@REM Licensed to the Apache Software Foundation (ASF) under one
@REM or more contributor license agreements.  See the NOTICE file
@REM distributed with this work for additional information
@REM regarding copyright ownership.  The ASF licenses this file
@REM to you under the Apache License, Version 2.0 (the
@REM "License"); you may not use this file except in compliance
@REM with the License.  You may obtain a copy of the License at
@REM
@REM    http://www.apache.org/licenses/LICENSE-2.0
@REM
@REM Unless required by applicable law or agreed to in writing,
@REM software distributed under the License is distributed on an
@REM "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
@REM KIND, either express or implied.  See the License for the
@REM specific language governing permissions and limitations
@REM under the License.
@REM ----------------------------------------------------------------------------

@REM ----------------------------------------------------------------------------
@REM Apache Maven Wrapper startup batch script, version 3.3.4
@REM
@REM Optional ENV vars
@REM   MVNW_REPOURL - repo url base for downloading maven distribution
@REM   MVNW_USERNAME/MVNW_PASSWORD - user and password for downloading maven
@REM   MVNW_VERBOSE - true: enable verbose log; others: silence the output
@REM ----------------------------------------------------------------------------

@IF "%__MVNW_ARG0_NAME__%"=="" (SET __MVNW_ARG0_NAME__=%~nx0)
@SET __MVNW_CMD__=
@SET __MVNW_ERROR__=
@SET __MVNW_PSMODULEP_SAVE=%PSModulePath%
@SET PSModulePath=
@FOR /F "usebackq tokens=1* delims==" %%A IN (`powershell -noprofile "& {$scriptDir='%~dp0'; $script='%__MVNW_ARG0_NAME__%'; icm -ScriptBlock ([Scriptblock]::Create((Get-Content -Raw '%~f0'))) -NoNewScope}"`) DO @(
  IF "%%A"=="MVN_CMD" (set __MVNW_CMD__=%%B) ELSE IF "%%B"=="" (echo %%A) ELSE (echo %%A=%%B)
)
@SET PSModulePath=%__MVNW_PSMODULEP_SAVE%
@SET __MVNW_PSMODULEP_SAVE=
@SET __MVNW_ARG0_NAME__=
@SET MVNW_USERNAME=
@SET MVNW_PASSWORD=
@IF NOT "%__MVNW_CMD__%"=="" ("%__MVNW_CMD__%" %*)
@echo Cannot start maven from wrapper >&2 && exit /b 1
@GOTO :EOF
: end batch / begin powershell #>

$ErrorActionPreference = "Stop"
if ($env:MVNW_VERBOSE -eq "true") {
  $VerbosePreference = "Continue"
}

# calculate distributionUrl, requires .mvn/wrapper/maven-wrapper.properties
$distributionUrl = (Get-Content -Raw "$scriptDir/.mvn/wrapper/maven-wrapper.properties" | ConvertFrom-StringData).distributionUrl
if (!$distributionUrl) {
  Write-Error "cannot read distributionUrl property in $scriptDir/.mvn/wrapper/maven-wrapper.properties"
}

switch -wildcard -casesensitive ( $($distributionUrl -replace '^.*/','') ) {
  "maven-mvnd-*" {
    $USE_MVND = $true
    $distributionUrl = $distributionUrl -replace '-bin\.[^.]*$',"-windows-amd64.zip"
    $MVN_CMD = "mvnd.cmd"
    break
  }
  default {
    $USE_MVND = $false
    $MVN_CMD = $script -replace '^mvnw','mvn'
    break
  }
}

# apply MVNW_REPOURL and calculate MAVEN_HOME
# maven home pattern: ~/.m2/wrapper/dists/{apache-maven-<version>,maven-mvnd-<version>-<platform>}/<hash>
if ($env:MVNW_REPOURL) {
  $MVNW_REPO_PATTERN = if ($USE_MVND -eq $False) { "/org/apache/maven/" } else { "/maven/mvnd/" }
  $distributionUrl = "$env:MVNW_REPOURL$MVNW_REPO_PATTERN$($distributionUrl -replace "^.*$MVNW_REPO_PATTERN",'')"
}
$distributionUrlName = $distributionUrl -replace '^.*/',''
$distributionUrlNameMain = $distributionUrlName -replace '\.[^.]*$','' -replace '-bin$',''

$MAVEN_M2_PATH = "$HOME/.m2"
if ($env:MAVEN_USER_HOME) {
  $MAVEN_M2_PATH = "$env:MAVEN_USER_HOME"
}

if (-not (Test-Path -Path $MAVEN_M2_PATH)) {
    New-Item -Path $MAVEN_M2_PATH -ItemType Directory | Out-Null
}

$MAVEN_WRAPPER_DISTS = $null
if ((Get-Item $MAVEN_M2_PATH).Target[0] -eq $null) {
  $MAVEN_WRAPPER_DISTS = "$MAVEN_M2_PATH/wrapper/dists"
} else {
  $MAVEN_WRAPPER_DISTS = (Get-Item $MAVEN_M2_PATH).Target[0] + "/wrapper/dists"
}

$MAVEN_HOME_PARENT = "$MAVEN_WRAPPER_DISTS/$distributionUrlNameMain"
$MAVEN_HOME_NAME = ([System.Security.Cryptography.SHA256]::Create().ComputeHash([byte[]][char[]]$distributionUrl) | ForEach-Object {$_.ToString("x2")}) -join ''
$MAVEN_HOME = "$MAVEN_HOME_PARENT/$MAVEN_HOME_NAME"

if (Test-Path -Path "$MAVEN_HOME" -PathType Container) {
  Write-Verbose "found existing MAVEN_HOME at $MAVEN_HOME"
  Write-Output "MVN_CMD=$MAVEN_HOME/bin/$MVN_CMD"
  exit $?
}

if (! $distributionUrlNameMain -or ($distributionUrlName -eq $distributionUrlNameMain)) {
  Write-Error "distributionUrl is not valid, must end with *-bin.zip, but found $distributionUrl"
}

# prepare tmp dir
$TMP_DOWNLOAD_DIR_HOLDER = New-TemporaryFile
$TMP_DOWNLOAD_DIR = New-Item -Itemtype Directory -Path "$TMP_DOWNLOAD_DIR_HOLDER.dir"
$TMP_DOWNLOAD_DIR_HOLDER.Delete() | Out-Null
trap {
  if ($TMP_DOWNLOAD_DIR.Exists) {
    try { Remove-Item $TMP_DOWNLOAD_DIR -Recurse -Force | Out-Null }
    catch { Write-Warning "Cannot remove $TMP_DOWNLOAD_DIR" }
  }
}

New-Item -Itemtype Directory -Path "$MAVEN_HOME_PARENT" -Force | Out-Null

# Download and Install Apache Maven
Write-Verbose "Couldn't find MAVEN_HOME, downloading and installing it ..."
Write-Verbose "Downloading from: $distributionUrl"
Write-Verbose "Downloading to: $TMP_DOWNLOAD_DIR/$distributionUrlName"

$webclient = New-Object System.Net.WebClient
if ($env:MVNW_USERNAME -and $env:MVNW_PASSWORD) {
  $webclient.Credentials = New-Object System.Net.NetworkCredential($env:MVNW_USERNAME, $env:MVNW_PASSWORD)
}
[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
$webclient.DownloadFile($distributionUrl, "$TMP_DOWNLOAD_DIR/$distributionUrlName") | Out-Null

# If specified, validate the SHA-256 sum of the Maven distribution zip file
$distributionSha256Sum = (Get-Content -Raw "$scriptDir/.mvn/wrapper/maven-wrapper.properties" | ConvertFrom-StringData).distributionSha256Sum
if ($distributionSha256Sum) {
  if ($USE_MVND) {
    Write-Error "Checksum validation is not supported for maven-mvnd. `nPlease disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties."
  }
  Import-Module $PSHOME\Modules\Microsoft.PowerShell.Utility -Function Get-FileHash
  if ((Get-FileHash "$TMP_DOWNLOAD_DIR/$distributionUrlName" -Algorithm SHA256).Hash.ToLower() -ne $distributionSha256Sum) {
    Write-Error "Error: Failed to validate Maven distribution SHA-256, your Maven distribution might be compromised. If you updated your Maven version, you need to update the specified distributionSha256Sum property."
  }
}

# unzip and move
Expand-Archive "$TMP_DOWNLOAD_DIR/$distributionUrlName" -DestinationPath "$TMP_DOWNLOAD_DIR" | Out-Null

# Find the actual extracted directory name (handles snapshots where filename != directory name)
$actualDistributionDir = ""

# First try the expected directory name (for regular distributions)
$expectedPath = Join-Path "$TMP_DOWNLOAD_DIR" "$distributionUrlNameMain"
$expectedMvnPath = Join-Path "$expectedPath" "bin/$MVN_CMD"
if ((Test-Path -Path $expectedPath -PathType Container) -and (Test-Path -Path $expectedMvnPath -PathType Leaf)) {
  $actualDistributionDir = $distributionUrlNameMain
}

# If not found, search for any directory with the Maven executable (for snapshots)
if (!$actualDistributionDir) {
  Get-ChildItem -Path "$TMP_DOWNLOAD_DIR" -Directory | ForEach-Object {
    $testPath = Join-Path $_.FullName "bin/$MVN_CMD"
    if (Test-Path -Path $testPath -PathType Leaf) {
      $actualDistributionDir = $_.Name
    }
  }
}

if (!$actualDistributionDir) {
  Write-Error "Could not find Maven distribution directory in extracted archive"
}

Write-Verbose "Found extracted Maven distribution directory: $actualDistributionDir"
Rename-Item -Path "$TMP_DOWNLOAD_DIR/$actualDistributionDir" -NewName $MAVEN_HOME_NAME | Out-Null
try {
  Move-Item -Path "$TMP_DOWNLOAD_DIR/$MAVEN_HOME_NAME" -Destination $MAVEN_HOME_PARENT | Out-Null
} catch {
  if (! (Test-Path -Path "$MAVEN_HOME" -PathType Container)) {
    Write-Error "fail to move MAVEN_HOME"
  }
} finally {
  try { Remove-Item $TMP_DOWNLOAD_DIR -Recurse -Force | Out-Null }
  catch { Write-Warning "Cannot remove $TMP_DOWNLOAD_DIR" }
}

Write-Output "MVN_CMD=$MAVEN_HOME/bin/$MVN_CMD"
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/BackendApplication.java">
public class BackendApplication {
⋮----
public static void main(String[] args) {
SpringApplication.run(BackendApplication.class, args);
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/config/WebConfig.java">
public class WebConfig implements WebMvcConfigurer {
⋮----
// application.properties の app.cors.allowed-origins を参照
⋮----
// Cookie/認証情報を送る必要がある場合だけ true
⋮----
@SuppressWarnings("null") // コンパイラのNull警告を抑制（Stream処理の配列変換等で発生するため）
public void addCorsMappings(@NonNull CorsRegistry registry) {
// 安全対策: nullの場合は空文字扱いにする
String safeOrigins = Objects.requireNonNullElse(allowedOrigins, "");
⋮----
String[] origins = Arrays.stream(safeOrigins.split(","))
.map(String::trim)
.filter(s -> !s.isEmpty())
.toArray(String[]::new);
⋮----
// APIだけに適用
registry.addMapping("/api/**")
.allowedOrigins(origins)
.allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
.allowedHeaders("Content-Type", "Authorization")
.allowCredentials(allowCredentials)
.maxAge(3600);
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/DataLoader.java">
/**
 * 初期データ投入用のクラスです。
 * PostgreSQL の実データ（つくばのデータ）を表示させるため、機能を完全に停止しています。
 */
// @Component
public class DataLoader implements CommandLineRunner {
⋮----
// 警告を消すため、使用していないリポジトリの宣言もコメントアウトしました
// private final ToiletRepository repository;
⋮----
// コンストラクタも現在は不要なためコメントアウトしています
/*
    public DataLoader(ToiletRepository repository) {
        this.repository = repository;
    }
    */
⋮----
public void run(String... args) throws Exception {
// 現在は何もしない設定です
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/EquipmentRepository.java">
public interface EquipmentRepository extends JpaRepository<Equipment, Long> {
// 特定のトイレIDに紐づく設備をすべて取得する
List<Equipment> findByToilet_Id(Long toiletId);
⋮----
// 特定のトイレの、特定の設備を探す（重複チェック用）
boolean existsByToilet_IdAndType(Long toiletId, EquipmentType type);
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/EquipmentType.java">
WHEELCHAIR,       // 車椅子
DIAPER,           // オムツ交換
OSTOMATE,         // オストメイト
WASHLET,          // ウォシュレット
NURSING_ROOM,     // 授乳室
VISUAL_SUPPORT,   // 視覚障害者対応
GENDER_SEPARATED, // 男女別
UNISEX,           // 男女共用
FREE,             // 無料
PAID,             // 有料
OPEN_24H,         // 24時間
PARKING           // 駐車場
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/exception/ErrorResponse.java">
public class ErrorResponse {
⋮----
// Validationなど「項目ごとのエラー」を返すため（必要なときだけ入る）
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/exception/GlobalExceptionHandler.java">
public class GlobalExceptionHandler {
⋮----
// 1) リソースが見つからない (404)
⋮----
public ResponseEntity<ErrorResponse> handleResourceNotFoundException(
⋮----
ErrorResponse error = new ErrorResponse(
HttpStatus.NOT_FOUND.value(),
ex.getMessage(),
LocalDateTime.now(),
request.getDescription(false),
⋮----
// 2) バリデーションエラー (400)
⋮----
public ResponseEntity<ErrorResponse> handleValidationExceptions(
⋮----
// FieldErrorだけを安全に取り出す
⋮----
ex.getBindingResult().getFieldErrors().forEach(fe ->
errors.put(fe.getField(), fe.getDefaultMessage())
⋮----
HttpStatus.BAD_REQUEST.value(),
⋮----
// 3) その他の予期せぬ例外 (500)
⋮----
public ResponseEntity<ErrorResponse> handleGlobalException(
⋮----
// 開発中はスタックトレースを出力
ex.printStackTrace();
⋮----
HttpStatus.INTERNAL_SERVER_ERROR.value(),
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/exception/ResourceNotFoundException.java">
public class ResourceNotFoundException extends RuntimeException {
⋮----
super(String.format("%s が見つかりません: %s='%s'", resourceName, fieldName, fieldValue));
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/HelloController.java">
public class HelloController {
⋮----
// 1. 一覧画面を表示する
⋮----
public String showToiletList(Model model) {
model.addAttribute("toilets", repository.findAll());
⋮----
// ★変更後：空っぽのデータを「toilet」という名前で渡す
⋮----
public String showAddForm(Model model) {
model.addAttribute("toilet", new Toilet());
⋮----
// ★追加：指定したIDのデータを検索して渡す（編集用）
⋮----
public String showEditForm(@PathVariable Long id, Model model) {
Toilet t = repository.findById(id).get(); // IDで検索
model.addAttribute("toilet", t);          // 見つかったデータを渡す
return "toilet-form";                     // 同じフォーム画面を使う
⋮----
// 3. フォームから送られてきたデータを保存する（新機能！）
⋮----
public String saveToilet(@ModelAttribute Toilet toilet) {
repository.save(toilet); // データベースに保存！
return "redirect:/";     // 保存したら一覧画面に戻る
⋮----
// 4. 指定したIDのトイレを削除する（新機能！）
⋮----
public String deleteToilet(@PathVariable Long id) {
repository.deleteById(id); // 指定されたIDのデータを削除する
return "redirect:/";       // 一覧画面に戻る
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/ToiletService.java">
import java.util.Objects; // ★追加: Nullチェック用
⋮----
public class ToiletService {
⋮----
// --- 検索ロジック ---
public List<Toilet> searchToilets(
⋮----
// 1. 緯度経度による半径検索
⋮----
return toiletRepository.findWithinRadius(lat, lng, r);
⋮----
// 2. 条件検索
if (facilityCategory != null || minCleanliness != null || keyword != null || (equipment != null && !equipment.isEmpty())) {
⋮----
if (equipment != null && !equipment.isEmpty()) {
⋮----
types.add(EquipmentType.valueOf(eq));
⋮----
// 無効な値は無視
⋮----
if (types.isEmpty()) types = null;
⋮----
return toiletRepository.searchBySpecs(facilityCategory, minCleanliness, keyword, types);
⋮----
// 3. 全件取得
return toiletRepository.findAll();
⋮----
// --- ID検索 ---
public Toilet getToilet(Long id) {
// ★修正: nullチェックを行い、警告を解消
Long safeId = Objects.requireNonNull(id, "ID must not be null");
⋮----
return toiletRepository.findById(safeId)
.orElseThrow(() -> new ResourceNotFoundException("トイレ", "id", safeId));
⋮----
// --- 新規登録 ---
public Toilet createToilet(Toilet toilet) {
if (toilet.getCleanliness() == null) {
toilet.setCleanliness(3);
⋮----
return toiletRepository.save(toilet);
⋮----
// --- 更新ロジック ---
public Toilet updateToilet(Long id, Toilet details) {
// getToilet内でnullチェック済みなので、ここはそのまま呼び出すだけで安全
Toilet toilet = getToilet(id);
⋮----
Optional.ofNullable(details.getName())
.filter(s -> !s.isBlank())
.ifPresent(toilet::setName);
⋮----
Optional.ofNullable(details.getAddress())
⋮----
.ifPresent(toilet::setAddress);
⋮----
Optional.ofNullable(details.getDescription())
.ifPresent(toilet::setDescription);
⋮----
Optional.ofNullable(details.getImage())
.ifPresent(toilet::setImage);
⋮----
Optional.ofNullable(details.getFacilityCategory())
.ifPresent(toilet::setFacilityCategory);
⋮----
Optional.ofNullable(details.getEquipment())
.ifPresent(toilet::setEquipment);
⋮----
Optional.ofNullable(details.getLat()).ifPresent(toilet::setLat);
Optional.ofNullable(details.getLng()).ifPresent(toilet::setLng);
Optional.ofNullable(details.getCleanliness()).ifPresent(toilet::setCleanliness);
⋮----
Optional.ofNullable(details.getWheelchair()).ifPresent(toilet::setWheelchair);
Optional.ofNullable(details.getDiaper()).ifPresent(toilet::setDiaper);
Optional.ofNullable(details.getOpen24h()).ifPresent(toilet::setOpen24h);
⋮----
// --- 削除 ---
public void deleteToilet(Long id) {
⋮----
if (!toiletRepository.existsById(safeId)) {
throw new ResourceNotFoundException("トイレ", "id", safeId);
⋮----
toiletRepository.deleteById(safeId);
</file>

<file path="backend/backend/src/main/resources/db/migration/V2__add_equipment_table.sql">
-- V2__add_equipment_table.sql
-- Phase 2-1: Equipmentテーブル追加＆既存データ移行（PostgreSQL）
-- 対応: 古いPostgreSQLでも動くように IF NOT EXISTS（CONSTRAINT）を使わない

BEGIN;

-- 1) equipment テーブル作成（無ければ作る）
CREATE TABLE IF NOT EXISTS equipment (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    toilet_id BIGINT NOT NULL,
    type VARCHAR(50) NOT NULL,
    CONSTRAINT fk_equipment_toilet
      FOREIGN KEY (toilet_id) REFERENCES toilet(id) ON DELETE CASCADE
);

-- 2) ユニーク制約（存在しなければ追加）
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint
    WHERE conname = 'uq_equipment_toilet_type'
  ) THEN
    ALTER TABLE equipment
      ADD CONSTRAINT uq_equipment_toilet_type UNIQUE (toilet_id, type);
  END IF;
END $$;

-- 3) 既存データ移行（重複挿入防止）
INSERT INTO equipment (toilet_id, type)
SELECT t.id, 'WHEELCHAIR'
FROM toilet t
WHERE t.wheelchair = true
  AND NOT EXISTS (
    SELECT 1 FROM equipment e
    WHERE e.toilet_id = t.id AND e.type = 'WHEELCHAIR'
  );

INSERT INTO equipment (toilet_id, type)
SELECT t.id, 'DIAPER'
FROM toilet t
WHERE t.diaper = true
  AND NOT EXISTS (
    SELECT 1 FROM equipment e
    WHERE e.toilet_id = t.id AND e.type = 'DIAPER'
  );

INSERT INTO equipment (toilet_id, type)
SELECT t.id, 'OPEN_24H'
FROM toilet t
WHERE t.open24h = true
  AND NOT EXISTS (
    SELECT 1 FROM equipment e
    WHERE e.toilet_id = t.id AND e.type = 'OPEN_24H'
  );

COMMIT;
</file>

<file path="backend/backend/src/main/resources/db/migration/V3__add_search_indexes.sql">
-- V3__add_search_indexes.sql
-- 検索パフォーマンス向上のためのインデックス追加

-- 1. 施設カテゴリ検索用 (WHERE facility_category = ?)
CREATE INDEX IF NOT EXISTS idx_toilet_facility_category 
ON toilet (facility_category);

-- 2. 清潔度フィルタリング用 (WHERE cleanliness >= ?)
CREATE INDEX IF NOT EXISTS idx_toilet_cleanliness 
ON toilet (cleanliness);

-- 3. 緯度経度用
CREATE INDEX IF NOT EXISTS idx_toilet_lat_lng 
ON toilet (lat, lng);

-- 4. 設備テーブルのタイプ検索用 (JOIN時の高速化)
CREATE INDEX IF NOT EXISTS idx_equipment_type 
ON equipment (type);

CREATE INDEX IF NOT EXISTS idx_equipment_toilet_id
ON equipment (toilet_id);
</file>

<file path="backend/backend/src/main/resources/db/migration/V4__insert_sample_data.sql">
-- V4__insert_sample_data.sql
-- つくば駅周辺のリアルなトイレデータを投入（検索機能検証用）

BEGIN;

-- 既存のテストデータがあれば削除（ID 100未満をテストデータとみなす）
DELETE FROM equipment WHERE toilet_id < 100;
DELETE FROM toilet WHERE id < 100;

-- シーケンスのリセット（IDを1から振るため）
ALTER SEQUENCE toilet_id_seq RESTART WITH 1;
ALTER SEQUENCE equipment_id_seq RESTART WITH 1;

-- =========================================================
-- 1. Toiletテーブルへのデータ投入
-- ※ facility_category と equipment(CSV) をしっかり入れるのがポイント
-- =========================================================

INSERT INTO toilet (
  name, address, lat, lng, description, cleanliness, 
  facility_category, 
  public_use, wheelchair, diaper, open24h, 
  equipment -- フロントエンド用CSV
) VALUES 
-- 1. 駅周辺 (Station)
('つくば駅 多目的トイレ', '茨城県つくば市吾妻2丁目', 36.0833, 140.1120, 'TXつくば駅改札外、出口A3付近。広くて綺麗です。', 5, 'station', true, true, true, true, 'wheelchair,diaper,ostomate,washlet,open_24h'),
('つくば駅 北口公衆トイレ', '茨城県つくば市吾妻1丁目', 36.0840, 140.1125, 'バスターミナル近く。24時間利用可能。', 3, 'station', true, true, false, true, 'wheelchair,open_24h'),
('研究学園駅 バリアフリートイレ', '茨城県つくば市研究学園5丁目', 36.0815, 140.0820, '改札内と改札外にあります。', 4, 'station', true, true, true, true, 'wheelchair,diaper,washlet,open_24h'),

-- 2. 商業施設 (Commercial)
('BiViつくば 2Fトイレ', '茨城県つくば市吾妻1-8-10', 36.0828, 140.1115, '授乳室も併設されています。非常に清潔。', 5, 'commercial', true, true, true, false, 'wheelchair,diaper,nursing_room,washlet'),
('イーアスつくば フードコート横', '茨城県つくば市研究学園5-19', 36.0860, 140.0810, '家族連れに最適。オストメイト対応。', 4, 'commercial', true, true, true, false, 'wheelchair,diaper,ostomate,washlet,parking'),
('LALAガーデンつくば 東館', '茨城県つくば市小野崎278-1', 36.0750, 140.1050, '駐車場から近くて便利です。', 3, 'commercial', true, false, true, true, 'diaper,open_24h,parking'),

-- 3. 公園 (Park)
('つくば中央公園 トイレ', '茨城県つくば市吾妻2-7-5', 36.0855, 140.1130, '公園の中央付近。夜間も利用可。', 3, 'park', true, true, false, true, 'wheelchair,open_24h'),
('洞峰公園 管理棟トイレ', '茨城県つくば市二の宮2-20', 36.0580, 140.1250, '温水プール利用時にも便利。', 4, 'park', true, true, true, true, 'wheelchair,diaper,open_24h'),
('松見公園 公衆トイレ', '茨城県つくば市天久保1-4', 36.0950, 140.1100, '展望塔の近く。古いですが清掃されています。', 2, 'park', true, false, false, true, 'open_24h'),

-- 4. 医療・公共 (Public/Medical)
('筑波メディカルセンター病院', '茨城県つくば市天久保1-3-1', 36.0920, 140.1110, '正面玄関近く。オストメイト完備。', 5, 'medical', true, true, true, false, 'wheelchair,diaper,ostomate,washlet'),
('つくば市役所 本庁舎', '茨城県つくば市研究学園1-1-1', 36.0780, 140.0800, '誰でも利用可能な多目的トイレ。', 5, 'public', true, true, true, false, 'wheelchair,diaper,ostomate,visual_support'),

-- 5. 宿泊施設 (Hotel)
('ホテル日航つくば ロビー', '茨城県つくば市吾妻1-1364-1', 36.0825, 140.1135, '宿泊客以外も利用可（スタッフに要声掛け）。高級感あり。', 5, 'hotel_tourism', false, true, true, false, 'wheelchair,diaper,washlet'),
('オークラフロンティア ホテル', '茨城県つくば市吾妻1丁目', 36.0830, 140.1140, 'エントランス付近。', 4, 'hotel_tourism', false, true, false, false, 'wheelchair,washlet'),

-- 6. その他 (Other)
('セブンイレブン つくば吾妻店', '茨城県つくば市吾妻3丁目', 36.0880, 140.1150, '店員に声をかけてから利用してください。', 3, 'convenience', true, false, false, true, 'open_24h'),
('ENEOS つくば学園SS', '茨城県つくば市竹園1丁目', 36.0800, 140.1180, '給油のついでに利用可能。', 3, 'other', true, false, false, true, 'open_24h,parking');


-- =========================================================
-- 2. Equipmentテーブルへのデータ投入
-- ※ バックエンド検索(searchBySpecs)用。Toiletテーブルと整合性を取る。
-- =========================================================

INSERT INTO equipment (toilet_id, type)
SELECT id, 'WHEELCHAIR' FROM toilet WHERE equipment LIKE '%wheelchair%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'DIAPER' FROM toilet WHERE equipment LIKE '%diaper%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'OSTOMATE' FROM toilet WHERE equipment LIKE '%ostomate%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'WASHLET' FROM toilet WHERE equipment LIKE '%washlet%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'NURSING_ROOM' FROM toilet WHERE equipment LIKE '%nursing_room%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'OPEN_24H' FROM toilet WHERE equipment LIKE '%open_24h%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'PARKING' FROM toilet WHERE equipment LIKE '%parking%';

INSERT INTO equipment (toilet_id, type)
SELECT id, 'VISUAL_SUPPORT' FROM toilet WHERE equipment LIKE '%visual_support%';

COMMIT;
</file>

<file path="backend/backend/src/main/resources/db/migration/V5__add_image_column.sql">
-- V5__add_image_column.sql
-- 画像URLを保存するためのカラムを追加
-- URLは長くなる可能性があるため TEXT 型を採用

ALTER TABLE toilet ADD COLUMN IF NOT EXISTS image TEXT;
</file>

<file path="backend/backend/src/main/resources/templates/toilet-form.html">
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>トイレ登録・編集</title>
</head>
<body>
    <h1>トイレ情報を入力</h1>
    
    <form action="/save" method="post" th:object="${toilet}">
        
        <input type="hidden" th:field="*{id}">
        
        <p>名前：<br>
        <input type="text" th:field="*{name}" required></p>
        
        <p>住所：<br>
        <input type="text" th:field="*{address}"></p>
        
        <p>説明：<br>
        <textarea th:field="*{description}" rows="3"></textarea></p>

        <hr>
        <h3>地図用の設定</h3>
        <p>緯度 (例: 36.0834)：<br>
        <input type="text" th:field="*{lat}"></p>

        <p>経度 (例: 140.1133)：<br>
        <input type="text" th:field="*{lng}"></p>

        <h3>設備情報</h3>
        <p>
            <label><input type="checkbox" th:field="*{wheelchair}"> 車椅子対応</label><br>
            <label><input type="checkbox" th:field="*{diaper}"> オムツ替えスペース</label><br>
            <label><input type="checkbox" th:field="*{open24h}"> 24時間利用可能</label>
        </p>
        <button type="submit">保存する</button>
    </form>
    
    <br>
    <a href="/">一覧に戻る</a>
</body>
</html>
</file>

<file path="backend/backend/src/main/resources/templates/toilet-list.html">
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>トイレ検索</title>
</head>
<body>
    <h1>トイレ一覧</h1>
    
	<table border="1">
	        <thead>
	            <tr>
	                <th>ID</th>
	                <th>名前</th>
	                <th>住所</th>
	                <th>説明</th>
	                <th>操作</th> </tr>
	        </thead>
	        <tbody>
	            <tr th:each="toilet : ${toilets}">
	                <td th:text="${toilet.id}">ID</td>
	                <td th:text="${toilet.name}">名前</td>
	                <td th:text="${toilet.address}">住所</td>
	                <td th:text="${toilet.description}">説明</td>
					<td style="white-space: nowrap">
					    <a th:href="@{/edit/{id}(id=${toilet.id})}">編集</a>						
						/						
					    <a th:href="@{/delete/{id}(id=${toilet.id})}">削除</a>
					</td>
	            </tr>
	        </tbody>
	    </table>
    
    <br>
    <a href="/new">新しいトイレを登録する</a>
</body>
</html>
</file>

<file path="backend/backend/src/test/java/com/imatoilet/backend/BackendApplicationTests.java">
class BackendApplicationTests {
⋮----
void contextLoads() {
</file>

<file path="backend/backend/src/test/java/com/imatoilet/backend/ToiletApiControllerTest.java">
class ToiletApiControllerTest {
⋮----
void shouldReturnListOfToilets() throws Exception {
// APIを叩いて検証
mockMvc.perform(get("/api/toilets"))
// 1. ステータスコードが 200 (OK) であること
.andExpect(status().isOk())
// 2. コンテンツタイプが JSON であること
.andExpect(content().contentType(MediaType.APPLICATION_JSON))
// 3. 返ってきたデータが「配列（リスト）」形式であること
.andExpect(jsonPath("$").isArray());
</file>

<file path="toilet-frontend/.env.example">
# Backend API URL
VITE_API_BASE_URL=http://localhost:8080/api/toilets

# Cloudinary Config (画像アップロード機能用)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# Google Maps API Key
# Caution: Never commit the actual API Key to GitHub!
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
</file>

<file path="toilet-frontend/eslint.config.js">

</file>

<file path="toilet-frontend/public/vite.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--logos" width="31.88" height="32" preserveAspectRatio="xMidYMid meet" viewBox="0 0 256 257"><defs><linearGradient id="IconifyId1813088fe1fbc01fb466" x1="-.828%" x2="57.636%" y1="7.652%" y2="78.411%"><stop offset="0%" stop-color="#41D1FF"></stop><stop offset="100%" stop-color="#BD34FE"></stop></linearGradient><linearGradient id="IconifyId1813088fe1fbc01fb467" x1="43.376%" x2="50.316%" y1="2.242%" y2="89.03%"><stop offset="0%" stop-color="#FFEA83"></stop><stop offset="8.333%" stop-color="#FFDD35"></stop><stop offset="100%" stop-color="#FFA800"></stop></linearGradient></defs><path fill="url(#IconifyId1813088fe1fbc01fb466)" d="M255.153 37.938L134.897 252.976c-2.483 4.44-8.862 4.466-11.382.048L.875 37.958c-2.746-4.814 1.371-10.646 6.827-9.67l120.385 21.517a6.537 6.537 0 0 0 2.322-.004l117.867-21.483c5.438-.991 9.574 4.796 6.877 9.62Z"></path><path fill="url(#IconifyId1813088fe1fbc01fb467)" d="M185.432.063L96.44 17.501a3.268 3.268 0 0 0-2.634 3.014l-5.474 92.456a3.268 3.268 0 0 0 3.997 3.378l24.777-5.718c2.318-.535 4.413 1.507 3.936 3.838l-7.361 36.047c-.495 2.426 1.782 4.5 4.151 3.78l15.304-4.649c2.372-.72 4.652 1.36 4.15 3.788l-11.698 56.621c-.732 3.542 3.979 5.473 5.943 2.437l1.313-2.028l72.516-144.72c1.215-2.423-.88-5.186-3.54-4.672l-25.505 4.922c-2.396.462-4.435-1.77-3.759-4.114l16.646-57.705c.677-2.35-1.37-4.583-3.769-4.113Z"></path></svg>
</file>

<file path="toilet-frontend/README.md">
# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
</file>

<file path="toilet-frontend/src/App.css">

</file>

<file path="toilet-frontend/src/assets/react.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--logos" width="35.93" height="32" preserveAspectRatio="xMidYMid meet" viewBox="0 0 256 228"><path fill="#00D8FF" d="M210.483 73.824a171.49 171.49 0 0 0-8.24-2.597c.465-1.9.893-3.777 1.273-5.621c6.238-30.281 2.16-54.676-11.769-62.708c-13.355-7.7-35.196.329-57.254 19.526a171.23 171.23 0 0 0-6.375 5.848a155.866 155.866 0 0 0-4.241-3.917C100.759 3.829 77.587-4.822 63.673 3.233C50.33 10.957 46.379 33.89 51.995 62.588a170.974 170.974 0 0 0 1.892 8.48c-3.28.932-6.445 1.924-9.474 2.98C17.309 83.498 0 98.307 0 113.668c0 15.865 18.582 31.778 46.812 41.427a145.52 145.52 0 0 0 6.921 2.165a167.467 167.467 0 0 0-2.01 9.138c-5.354 28.2-1.173 50.591 12.134 58.266c13.744 7.926 36.812-.22 59.273-19.855a145.567 145.567 0 0 0 5.342-4.923a168.064 168.064 0 0 0 6.92 6.314c21.758 18.722 43.246 26.282 56.54 18.586c13.731-7.949 18.194-32.003 12.4-61.268a145.016 145.016 0 0 0-1.535-6.842c1.62-.48 3.21-.974 4.76-1.488c29.348-9.723 48.443-25.443 48.443-41.52c0-15.417-17.868-30.326-45.517-39.844Zm-6.365 70.984c-1.4.463-2.836.91-4.3 1.345c-3.24-10.257-7.612-21.163-12.963-32.432c5.106-11 9.31-21.767 12.459-31.957c2.619.758 5.16 1.557 7.61 2.4c23.69 8.156 38.14 20.213 38.14 29.504c0 9.896-15.606 22.743-40.946 31.14Zm-10.514 20.834c2.562 12.94 2.927 24.64 1.23 33.787c-1.524 8.219-4.59 13.698-8.382 15.893c-8.067 4.67-25.32-1.4-43.927-17.412a156.726 156.726 0 0 1-6.437-5.87c7.214-7.889 14.423-17.06 21.459-27.246c12.376-1.098 24.068-2.894 34.671-5.345a134.17 134.17 0 0 1 1.386 6.193ZM87.276 214.515c-7.882 2.783-14.16 2.863-17.955.675c-8.075-4.657-11.432-22.636-6.853-46.752a156.923 156.923 0 0 1 1.869-8.499c10.486 2.32 22.093 3.988 34.498 4.994c7.084 9.967 14.501 19.128 21.976 27.15a134.668 134.668 0 0 1-4.877 4.492c-9.933 8.682-19.886 14.842-28.658 17.94ZM50.35 144.747c-12.483-4.267-22.792-9.812-29.858-15.863c-6.35-5.437-9.555-10.836-9.555-15.216c0-9.322 13.897-21.212 37.076-29.293c2.813-.98 5.757-1.905 8.812-2.773c3.204 10.42 7.406 21.315 12.477 32.332c-5.137 11.18-9.399 22.249-12.634 32.792a134.718 134.718 0 0 1-6.318-1.979Zm12.378-84.26c-4.811-24.587-1.616-43.134 6.425-47.789c8.564-4.958 27.502 2.111 47.463 19.835a144.318 144.318 0 0 1 3.841 3.545c-7.438 7.987-14.787 17.08-21.808 26.988c-12.04 1.116-23.565 2.908-34.161 5.309a160.342 160.342 0 0 1-1.76-7.887Zm110.427 27.268a347.8 347.8 0 0 0-7.785-12.803c8.168 1.033 15.994 2.404 23.343 4.08c-2.206 7.072-4.956 14.465-8.193 22.045a381.151 381.151 0 0 0-7.365-13.322Zm-45.032-43.861c5.044 5.465 10.096 11.566 15.065 18.186a322.04 322.04 0 0 0-30.257-.006c4.974-6.559 10.069-12.652 15.192-18.18ZM82.802 87.83a323.167 323.167 0 0 0-7.227 13.238c-3.184-7.553-5.909-14.98-8.134-22.152c7.304-1.634 15.093-2.97 23.209-3.984a321.524 321.524 0 0 0-7.848 12.897Zm8.081 65.352c-8.385-.936-16.291-2.203-23.593-3.793c2.26-7.3 5.045-14.885 8.298-22.6a321.187 321.187 0 0 0 7.257 13.246c2.594 4.48 5.28 8.868 8.038 13.147Zm37.542 31.03c-5.184-5.592-10.354-11.779-15.403-18.433c4.902.192 9.899.29 14.978.29c5.218 0 10.376-.117 15.453-.343c-4.985 6.774-10.018 12.97-15.028 18.486Zm52.198-57.817c3.422 7.8 6.306 15.345 8.596 22.52c-7.422 1.694-15.436 3.058-23.88 4.071a382.417 382.417 0 0 0 7.859-13.026a347.403 347.403 0 0 0 7.425-13.565Zm-16.898 8.101a358.557 358.557 0 0 1-12.281 19.815a329.4 329.4 0 0 1-23.444.823c-7.967 0-15.716-.248-23.178-.732a310.202 310.202 0 0 1-12.513-19.846h.001a307.41 307.41 0 0 1-10.923-20.627a310.278 310.278 0 0 1 10.89-20.637l-.001.001a307.318 307.318 0 0 1 12.413-19.761c7.613-.576 15.42-.876 23.31-.876H128c7.926 0 15.743.303 23.354.883a329.357 329.357 0 0 1 12.335 19.695a358.489 358.489 0 0 1 11.036 20.54a329.472 329.472 0 0 1-11 20.722Zm22.56-122.124c8.572 4.944 11.906 24.881 6.52 51.026c-.344 1.668-.73 3.367-1.15 5.09c-10.622-2.452-22.155-4.275-34.23-5.408c-7.034-10.017-14.323-19.124-21.64-27.008a160.789 160.789 0 0 1 5.888-5.4c18.9-16.447 36.564-22.941 44.612-18.3ZM128 90.808c12.625 0 22.86 10.235 22.86 22.86s-10.235 22.86-22.86 22.86s-22.86-10.235-22.86-22.86s10.235-22.86 22.86-22.86Z"></path></svg>
</file>

<file path="toilet-frontend/src/components.css">
/* =========================
   Buttons
========================= */
.btn{
⋮----
max-width: 420px;        /* ← 横に伸びすぎない */
⋮----
/* PCでは中央寄せ */
⋮----
/* 押した感 */
.btn:active{
⋮----
/* キーボード操作対応 */
.btn:focus-visible{
⋮----
/* =========================
   Primary
========================= */
.btn-primary{
⋮----
/* 追加 */
⋮----
.btn-primary:hover{
⋮----
/* =========================
   Secondary
========================= */
.btn-secondary{
⋮----
.btn-secondary:hover{
⋮----
/* =========================
   Sub
========================= */
.btn-sub{
⋮----
.btn-sub:hover{
⋮----
/* =========================
   Disabled
========================= */
.btn-disabled{
</file>

<file path="toilet-frontend/src/components/ErrorBoundary.jsx">
class ErrorBoundary extends React.Component {
⋮----
static getDerivedStateFromError(error)
⋮----
componentDidCatch(error, errorInfo)
⋮----
render()
⋮----
{/* 開発環境でのみエラー詳細を表示 */}
</file>

<file path="toilet-frontend/src/components/Header.jsx">
function Header()
⋮----
{/* Linkタグを使うと、ページを再読み込みせずにサッと移動できます */}
</file>

<file path="toilet-frontend/src/components/Header.test.jsx">
import '@testing-library/jest-dom'; // 便利な判定コマンド（toBeInTheDocumentなど）を使えるようにする
⋮----
// Headerコンポーネントを描画（Routerで包む必要がある）
⋮----
// 画面上に指定したテキストが存在するか確認
⋮----
// リンク（aタグ）を探し、href属性が "/" であるか確認
</file>

<file path="toilet-frontend/src/components/ToiletCard.jsx">
function ToiletCard(
</file>

<file path="toilet-frontend/src/components/ToiletCard.test.jsx">
// テスト用のダミーデータを作成
⋮----
publicUse: true,     // 「公共」タグが出るはず
wheelchair: true,    // 「多目的」タグが出るはず
diaper: false,       // 「オムツ交換」タグは出ないはず
⋮----
// 名前と住所が表示されているか確認
⋮----
// trueのものは画面にあるはず
⋮----
// falseのものは画面にないはず（queryByTextは、見つからない場合にnullを返すのでエラーにならない）
⋮----
// 「詳細を見る」リンクを取得して、href属性をチェック
</file>

<file path="toilet-frontend/src/config/api.js">
// 環境変数 (VITE_API_BASE_URL) を読み込んで定数としてエクスポートします
// .env ファイルがない場合や読み込めない場合の保険として、|| の後ろにデフォルト値を書いています
</file>

<file path="toilet-frontend/src/mocks/handlers.js">
// APIのエンドポイントと、返すデータを定義します
⋮----
// http.get(URL, リゾルバ関数)
// Detail.jsx がリクエストするURLと一致させます
⋮----
// 偽のレスポンス（JSON）を返します
</file>

<file path="toilet-frontend/src/mocks/server.js">
// リクエストを受け付けるモックサーバーを作成します
</file>

<file path="toilet-frontend/src/pages/Favorites.css">
.fav-main {
⋮----
/* コントロールエリア（並び替えや削除ボタンがある場所） */
.fav-controls {
⋮----
.select-wrap {
⋮----
.select-sort {
⋮----
/* リストエリア（カードが並ぶ場所） */
.list-area {
⋮----
/* カードのデザイン（検索結果と似ていますが、削除ボタン用に行を分けています） */
.fav-card {
⋮----
.fav-card:hover {
⋮----
.fav-card-name {
⋮----
.fav-card-meta {
⋮----
.fav-card-actions {
⋮----
/* ボタンの微調整 */
.btn-sm {
⋮----
.btn-danger-outline {
.btn-danger-outline:hover {
⋮----
/* お気に入りが空のときの表示 */
.fav-empty {
</file>

<file path="toilet-frontend/src/pages/Home.test.jsx">
// テキストの一部だけで検索する場合は正規表現（/text/）を使うと便利です
⋮----
// 特定のテキストを持つリンクを探す
⋮----
// リンク先が正しいか確認
</file>

<file path="toilet-frontend/src/pages/Register.css">
/* =========================
   register page
========================= */
.register-page {
⋮----
.register-grid {
⋮----
/* パネルスタイル */
.panel {
⋮----
.panel__head {
⋮----
.panel__head--tight {
⋮----
.panel__body {
⋮----
.panel__title {
⋮----
.panel__title--small {
⋮----
.panel__sub {
⋮----
.req {
⋮----
/* フォーム要素 */
.form-row { margin-bottom: 14px; }
.form-label {
⋮----
.input, .textarea {
.textarea { resize: vertical; }
⋮----
.input:focus, .textarea:focus {
⋮----
/* チェックボックス群 */
.checks {
⋮----
.check-label {
.check-label:hover {
.check-label input {
⋮----
/* 地図エリア */
.panel--map {
⋮----
.panel--map { position: static; }
⋮----
.reg-map-area {
⋮----
.map-hint {
⋮----
.dot {
⋮----
.form-footer {
</file>

<file path="toilet-frontend/src/setupTests.js">
// テスト開始前に、偽のAPIサーバーを起動します
⋮----
// 各テストが終わるごとに、ハンドラーの状態をリセットします
// (あるテストでイレギュラーな設定をしても、次のテストに影響させないため)
⋮----
// 全てのテストが終了したら、サーバーを停止します
</file>

<file path="toilet-frontend/src/utils.test.js">
// 1. 距離計算のテスト
⋮----
// つくば駅: 36.083, 140.112
// 秋葉原駅: 35.698, 139.774
⋮----
// 結果が 40km以上、60km未満なら計算式は合っているとみなす
⋮----
// 2. ID生成のテスト
⋮----
// 3. LocalStorage保存・読み込みのテスト
⋮----
// 各テストの前に、ブラウザの記憶領域(mock)を空っぽにする
⋮----
// 保存を実行
⋮----
// 読み込みを実行
⋮----
// 検証
</file>

<file path="toilet-frontend/vite.config.js">
// https://vite.dev/config/
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/Equipment.java">
import com.fasterxml.jackson.annotation.JsonIgnore; // ★追加
import lombok.EqualsAndHashCode; // ★追加
import lombok.ToString; // ★追加
⋮----
public class Equipment {
⋮----
// 修正後:
⋮----
@JsonIgnore             // ★親に戻らない（重要）
@ToString.Exclude       // ★Lombokの無限ループ防止
@EqualsAndHashCode.Exclude // ★Lombokの無限ループ防止
⋮----
// 設備の種類
⋮----
// コンストラクタ
</file>

<file path="toilet-frontend/.gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Security (Environmental Variables)
# .env で始まるファイルはすべて無視するが、exampleだけは許可する
.env
.env.*
!.env.example
</file>

<file path="toilet-frontend/index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>toilet-frontend</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
</file>

<file path="toilet-frontend/src/main.jsx">
import ErrorBoundary from './components/ErrorBoundary.jsx' // 追加
⋮----
<ErrorBoundary> {/* アプリ全体を囲む */}
</file>

<file path="toilet-frontend/src/pages/Detail.test.jsx">
import { http, HttpResponse } from 'msw';        // 追加
import { server } from '../mocks/server';        // 追加
⋮----
// このテストの間だけ、APIがエラー(500)を返すように設定を上書き
⋮----
// エラー発生後、「データが見つかりませんでした」が表示されるのを待つ
</file>

<file path="toilet-frontend/src/pages/Favorites.jsx">
import { loadUserToilets } from '../utils'; // 自分で登録したデータを読み込む関数
import './Favorites.css'; // さっき作ったCSSを読み込み
⋮----
// アイコン素材の読み込み
⋮----
function Favorites()
⋮----
// --- 状態管理 (State) ---
const [favorites, setFavorites] = useState([]); // 表示するトイレデータ一覧
const [loading, setLoading] = useState(true);   // 読み込み中かどうか
const [sortMode, setSortMode] = useState('updated'); // 並び順 ('updated' か 'name')
⋮----
const navigate = useNavigate(); // ページ移動用
const FAV_KEY = "imatoilet_favorites"; // LocalStorageの保存キー名
⋮----
// --- 1. 画面が開かれたときの処理 ---
⋮----
async function fetchFavorites()
⋮----
// (1) LocalStorageから「お気に入り登録したIDのリスト」だけ取り出す
// 例: ["1", "u_12345"]
⋮----
// (2) すべてのトイレデータ（API + ローカル登録）を用意する
⋮----
// バックエンドAPIから取得
⋮----
// 自分で登録したデータを取得して合体
⋮----
// (3) 全データの中から、お気に入りIDに含まれるものだけを抽出する (フィルタリング)
// 注意: IDの型(数値/文字列)がズレないよう String() で変換して比較
⋮----
// --- 2. 並び替え処理 ---
const getSortedFavorites = () =>
⋮----
// 元の配列を壊さないように [...favorites] でコピーしてからソート
⋮----
// 名前順 (あいうえお順)
⋮----
// 更新日順 (日付文字列の比較)
// データがない場合は古いものとして扱う
⋮----
return dateB.localeCompare(dateA); // 降順（新しい順）
⋮----
// --- 3. 削除処理 ---
const handleRemove = (id) =>
⋮----
// 画面上のリストから削除
⋮----
// LocalStorageのIDリストからも削除
⋮----
// --- 4. 全削除処理 ---
const handleClearAll = () =>
⋮----
setFavorites([]); // 画面を空に
localStorage.setItem(FAV_KEY, JSON.stringify([])); // 保存データも空に
⋮----
// 並び替え済みのデータを用意
⋮----
{/* 上部ナビ */}
⋮----
{/* 操作パネル */}
⋮----
{/* リスト表示エリア */}
⋮----
{/* 詳細ボタン：クリックで詳細ページへ移動 */}
⋮----
{/* 削除ボタン */}
</file>

<file path="toilet-frontend/src/pages/Search/ListPanel.jsx">
function ListPanel(
⋮----
{/* 上段：名前と距離を横並びにする */}
⋮----
{/* 下段：アイコン群（ここだけが横にスワイプできるようになる） */}
⋮----
{/* 1. 施設カテゴリ */}
⋮----
{/* 2. 基本フラグ */}
⋮----
{/* 3. 追加設備 (カンマ区切りのequipment文字列をチェック) */}
</file>

<file path="toilet-frontend/src/pages/Search/PlaceSearch.jsx">
// アイコン
⋮----
function PlaceSearch({ 
  placeQuery, 
  setPlaceQuery, 
  handlePlaceSearch, 
  handleCurrentLocation,
  handleKeywordSearch, // ★追加
  searchStatus,
  searchHistory,       // ★追加
  handleHistorySearch  // ★追加
})
⋮----
handleKeywordSearch, // ★追加
⋮----
searchHistory,       // ★追加
handleHistorySearch  // ★追加
⋮----
// onBlur={() => setTimeout(() => setShowHistory(false), 200)} // クリック判定用ディレイ
onKeyDown={(e) => e.key === 'Enter' && handleKeywordSearch()} // Enter時はキーワード検索優先
⋮----
{/* ★追加: 履歴のポップアップ表示 */}
⋮----
{/* 地図検索（Nominatim） */}
⋮----
{/* ★追加: キーワード検索（DB直接） */}
⋮----
{/* 現在地リセット */}
</file>

<file path="toilet-frontend/src/utils.js">
// 環境変数 (VITE_API_BASE_URL) を読み込んで定数としてエクスポート
⋮----
// ユーザー登録データを読み込む
export function loadUserToilets()
⋮----
// ユーザー登録データを保存する
export function saveUserToilets(arr)
⋮----
// ID生成
export function makeId()
⋮----
// 現在日付 (YYYY-MM-DD)
export function nowISO()
⋮----
// 2点間の距離計算 (Haversine formula)
export function calcDistance(lat1, lng1, lat2, lng2)
⋮----
const R = 6371; // km
⋮----
// ★追加: Cloudinaryへの画像アップロード関数
export async function uploadToCloudinary(file)
⋮----
// Cloudinaryへ送信
⋮----
return data.secure_url; // httpsから始まるURLを返す
</file>

<file path="toilet-frontend/vitest.config.js">
environment: 'jsdom', // ブラウザのふりをする設定
globals: true,        // describeやtestをimportなしで使えるようにする
setupFiles: './src/setupTests.js', // ★この行を追加（セットアップファイルを読み込む）
</file>

<file path="backend/backend/pom.xml">
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.9</version>
    <relativePath/> </parent>
  <groupId>com.imatoilet</groupId>
  <artifactId>backend</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>backend</name>
  <description>Imatoilet Backend API</description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/ToiletRepository.java">
public interface ToiletRepository extends JpaRepository<Toilet, Long> {
⋮----
// N+1対策済みの全件取得
⋮----
List<Toilet> findAll();
⋮----
// 1. 位置情報検索 (PostgreSQLネイティブクエリ / 半径km指定)
⋮----
List<Toilet> findWithinRadius(
⋮----
// 2. 条件検索 (JPQL / 正規化されたEquipmentテーブルとのJOIN)
// S0フェーズ: EquipmentTypeのリストに含まれる設備を持つトイレを検索
// キーワードは name, address, description, equipment(CSV) を対象にする
⋮----
List<Toilet> searchBySpecs(
</file>

<file path="toilet-frontend/src/App.jsx">
import Edit from './pages/Edit'; // ★追加
⋮----
function App()
⋮----
<Route path="/edit/:id" element={<Edit />} /> {/* ★追加 */}
</file>

<file path="toilet-frontend/src/base.css">
:root {
⋮----
/* 背景色を一元管理（#f5f7fb を適用） */
⋮----
* {
⋮----
body {
⋮----
/* 変数 --bg を使用して背景色を指定 */
⋮----
a {
⋮----
/* =========================
   レイアウト：センターカラム (Fluid Padding)
========================= */
.container {
⋮----
/* =========================
   トップページ専用スタイル
========================= */
.home-wrap {
⋮----
.home-grid {
⋮----
.brand-area {
⋮----
.brand-logo {
⋮----
.brand-logo .icon {
⋮----
.brand-sub {
⋮----
.menu-area {
⋮----
.menu-btn {
⋮----
.menu-btn:last-child {
⋮----
.menu-btn:hover {
⋮----
.menu-btn.primary {
⋮----
.menu-btn.primary:hover {
⋮----
.btn-icon {
⋮----
.btn-arrow {
⋮----
/* =========================
   詳細ページ (Detail.jsx) - Fluid & Grid対応
========================= */
.detail-main {
⋮----
.detail-nav {
⋮----
.back-link {
⋮----
.back-link:hover {
⋮----
.detail-card {
⋮----
.detail-image {
⋮----
.detail-image img {
⋮----
.detail-content {
⋮----
.detail-header {
⋮----
.detail-title {
⋮----
.fav-btn {
⋮----
.fav-btn:hover {
⋮----
.detail-tags {
⋮----
.tag {
⋮----
.tag-ok {
⋮----
.detail-info {
⋮----
.info-label {
⋮----
.info-text {
⋮----
.nav-btn {
⋮----
.nav-btn:hover {
⋮----
.detail-actions {
⋮----
/* アクセシビリティ対応：動きを減らす設定が有効な場合、アニメーションを無効化 */
⋮----
*,
</file>

<file path="toilet-frontend/src/index.css">
/* =========================================
   css/pages/index.css
   トップページ専用スタイル（スマホ・PC共通）
========================================= */
⋮----
/* --- 全体のレイアウト調整 --- */
body {
⋮----
background-color: #f9f9f9; /* 全体の背景色 */
⋮----
.top-main {
⋮----
min-height: 80vh; /* 画面の高さを確保 */
⋮----
/* --- 1. ヘッダー（ロゴ） --- */
.site-header {
⋮----
.logo {
⋮----
justify-content: center; /* スマホは中央寄せ */
⋮----
.logo-icon {
.logo-now {
.logo-main { color: #1e88e5; }
⋮----
/* --- 2. ヒーローエリア（キャッチコピー） --- */
.hero {
⋮----
/* マージンやパディングも可変にして、狭い画面での圧迫感を減らす */
⋮----
.catch {
⋮----
/* スマホ 1.8rem -> PC 3.2rem まで滑らかに拡大 */
⋮----
.sub {
⋮----
/* スマホ 0.95rem -> PC 1.2rem まで滑らかに拡大 */
⋮----
.hero-img {
⋮----
/* スマホ 120px -> PC 260px まで滑らかに拡大 */
⋮----
/* --- 3. アクションエリア（ボタン群） --- */
.actions {
⋮----
margin: 0 auto; /* 中央寄せ */
⋮----
.btn-primary, .btn-secondary {
⋮----
.sub-actions {
⋮----
.sub-actions .btn {
⋮----
flex: 1; /* 等幅にする */
⋮----
.btn.btn-register {
.register-note {
⋮----
/* --- 4. 使い方エリア --- */
.howto {
⋮----
.howto-title {
⋮----
.howto-steps {
⋮----
.howto-steps li {
.howto-steps li:last-child {
⋮----
.step-num {
⋮----
.step-head {
.step-text {
⋮----
.howto-cta {
.howto-toplink {
.howto-note {
⋮----
/* =========================================
   ★PC用レイアウト（画面幅900px以上）
========================================= */
⋮----
.site-header .container {
⋮----
.top-main .container {
⋮----
/* Flexboxで縦に並べつつ、配置を制御 */
</file>

<file path="toilet-frontend/src/pages/Conditions.jsx">
// アイコン
⋮----
import DirectionsCarIcon from '@mui/icons-material/DirectionsCar'; // ★追加
⋮----
// カテゴリアイコン
⋮----
function Conditions()
⋮----
// --- 状態管理 ---
// 1. 旧仕様のフィルター
⋮----
// 2. 新仕様のカテゴリー
⋮----
// 3. 新仕様の設備条件
⋮----
parking: false, // ★追加
⋮----
// --- ハンドラ ---
const handleFilterChange = (e) =>
⋮----
const handleNewConditionChange = (e) =>
⋮----
const handleSearch = () =>
⋮----
// 1. 既存フラグの追加
⋮----
// 2. 新カテゴリの追加
⋮----
// 3. 新設備の追加 (parkingもここで自動的に追加される)
⋮----
// 施設カテゴリオプション定義
⋮----
{/* ヘッダー */}
⋮----
{/* フォーム全体 */}
⋮----
{/* ★追加：駐車場（目立たせる） */}
⋮----
function FilterChip(
</file>

<file path="toilet-frontend/src/pages/Home.jsx">
function Home()
⋮----
// 現在地を取得して検索ページへ飛ぶハンドラ
const handleCurrentLocationSearch = () =>
⋮----
// 取得した座標を持って検索ページへ移動
// radiusはバックエンドのデフォルト(5km)に任せるため指定しない、または明示的に送る
⋮----
{/* 画像を表示: ファーストビューなのでlazyは使わず、asyncのみ適用 */}
⋮----
{/* 現在地検索ボタン */}
⋮----
{/* ✅ 副CTA：トイレ登録 */}
⋮----
{/* 使い方 */}
</file>

<file path="toilet-frontend/src/pages/Search/FilterPanel.jsx">
// --- 必須アイコンのインポート（すべて網羅） ---
⋮----
function FilterPanel({ 
  placeQuery, setPlaceQuery, handleKeywordSearch, handleCurrentLocation,
  handlePlaceSearch, searchHistory, handleHistorySearch
})
⋮----
const [isOpen, setIsOpen] = useState(false); // アコーディオン用
⋮----
// ポップアップの外側を判定するための ref
⋮----
// 外側クリックとEscキーでポップアップを閉じる安全なロジック
⋮----
const handleClickOutside = (event) =>
⋮----
const handleKeyDown = (event) =>
⋮----
// URLパラメータ更新用ヘルパー
const updateParam = (key, value) =>
⋮----
const getBoolParam = (key)
const getStrParam = (key)
⋮----
{/* 1. キーワード・場所検索エリア */}
⋮----
{/* 履歴ポップアップ */}
⋮----
{/* スマホ用のアコーディオン開閉ボタン */}
⋮----
{/* 開閉するエリア */}
⋮----
{/* 2. 施設タイプ */}
⋮----
{/* 3. 設備・条件 */}
⋮----
// トグルスイッチ用の部品コンポーネント
function CheckRow(
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/Toilet.java">
// ★追加1：リストを使うためのインポート
⋮----
public class Toilet {
⋮----
// --- 既存フィールド ---
⋮----
// --- 新設計フィールド ---
⋮----
private String equipment; // ※これはCSV用として残します
⋮----
// ★追加: 画像URL (TEXT型に対応)
⋮----
// ★追加2：equipmentテーブルと繋がるリスト（N+1対策の窓口）
⋮----
@ToString.Exclude           // エラー防止（無限ループ対策）
@EqualsAndHashCode.Exclude  // エラー防止（無限ループ対策）
⋮----
// --- 互換性メソッド ---
⋮----
public void syncCompatibility() {
syncOldToNew();
syncNewToOld();
⋮----
private void syncOldToNew() {
if (this.facilityCategory == null || this.facilityCategory.isEmpty()) {
if (Boolean.TRUE.equals(typeStation)) this.facilityCategory = "station";
else if (Boolean.TRUE.equals(typePark)) this.facilityCategory = "park";
else if (Boolean.TRUE.equals(typeMall)) this.facilityCategory = "commercial";
⋮----
if (Boolean.TRUE.equals(wheelchair) && !currentEq.contains("wheelchair")) {
currentEq = addTag(currentEq, "wheelchair");
⋮----
if (Boolean.TRUE.equals(diaper) && !currentEq.contains("diaper_table")) {
currentEq = addTag(currentEq, "diaper_table");
⋮----
if (Boolean.TRUE.equals(open24h) && !currentEq.contains("open_24h")) {
currentEq = addTag(currentEq, "open_24h");
⋮----
private void syncNewToOld() {
⋮----
if (this.facilityCategory.equals("station")) this.typeStation = true;
else if (this.facilityCategory.equals("park")) this.typePark = true;
else if (this.facilityCategory.equals("commercial")) this.typeMall = true;
⋮----
if (this.equipment.contains("wheelchair")) this.wheelchair = true;
if (this.equipment.contains("diaper_table")) this.diaper = true;
if (this.equipment.contains("open_24h")) this.open24h = true;
⋮----
private String addTag(String current, String tag) {
if (current == null || current.isEmpty()) {
</file>

<file path="toilet-frontend/src/components/SafeGoogleMap.jsx">
// ★修正: 'marker' ライブラリを復活させました（AdvancedMarkerElementに必須）
⋮----
export const SafeGoogleMap = (
⋮----
// 1. Google Maps SDKのロード設定
⋮----
// 2. gm_authFailure の検知
⋮----
window.gm_authFailure = () =>
⋮----
// --- エラー時の表示 ---
⋮----
// --- ロード中 ---
⋮----
// --- 正常表示 ---
⋮----
// ★追加: AdvancedMarkerElementを利用するためにマップIDを指定
</file>

<file path="toilet-frontend/src/pages/Search/index.jsx">
function Search()
⋮----
realLocation, // ★追加: フックから受け取る
⋮----
realLocation={realLocation} // ★重要: MapPanelにこれを渡さないとGPS機能が動きません
</file>

<file path="toilet-frontend/package.json">
{
  "name": "toilet-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.3.7",
    "@mui/material": "^7.3.7",
    "@react-google-maps/api": "^2.20.8",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.12.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "jsdom": "^27.4.0",
    "msw": "^2.12.7",
    "vite": "npm:rolldown-vite@7.2.5",
    "vitest": "^4.0.18"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
</file>

<file path="toilet-frontend/src/pages/Edit.jsx">
// ★Google Maps用
⋮----
// アイコン
⋮----
import DeleteIcon from '@mui/icons-material/Delete'; // ★削除アイコン追加
⋮----
function Edit()
⋮----
// フォームの状態
⋮----
images: [], // ★変更: 単一のimage文字列ではなく、画像URLの配列で管理
⋮----
// --- 1. データ読み込み ---
⋮----
async function fetchData()
⋮----
// eslint-disable-next-line react-hooks/exhaustive-deps
⋮----
const applyDataToForm = (data) =>
⋮----
// ★変更: カンマ区切りの文字列を配列に変換してセット
⋮----
images: imageList, // ★配列としてセット
⋮----
// ★Google Maps: 地図をクリックした時の処理
const handleMapClick = (e) =>
⋮----
// ★Google Maps: ピンをドラッグした時の処理
const handleMarkerDragEnd = (e) =>
⋮----
const handleChange = (e) =>
⋮----
// ★変更: 画像アップロードハンドラ (複数対応・追記型)
const handleFileChange = async (e) =>
⋮----
// 並列アップロード
⋮----
// ★重要: 既存の images 配列の後ろに新しい URL を追加する (上書きしない)
⋮----
// ★追加: 個別の画像を削除するハンドラ
const handleRemoveImage = (indexToRemove) =>
⋮----
const handleStarClick = (rating) =>
⋮----
const handleSubmit = async (e) =>
⋮----
// ★変更: 配列をカンマ区切り文字列に戻す
⋮----
image: imageStr, // ★更新用の文字列
⋮----
multiple // ★複数選択OK
⋮----
{/* ★追加: 編集画面用の画像プレビュー＆削除エリア */}
⋮----
{/* 削除ボタン */}
</file>

<file path="toilet-frontend/src/search.css">
/* =========================================================
   search.css (3カラム・フルハイト版 + モバイル対応)
   Performance Optimized Version
========================================================= */
⋮----
/* ページ全体のラッパー：ヘッダー以外の高さを確保 */
.search-page-wrapper {
⋮----
height: calc(100vh - 64px); /* ヘッダーの高さを引く */
overflow: hidden; /* 全体スクロールはさせない（PC時） */
⋮----
/* 既存コンテナの無効化（検索ページ内のみ） */
.search-page-wrapper .container {
⋮----
/* --- 3カラムレイアウト定義 (PC用デフォルト) --- */
.three-col-layout {
⋮----
/* 左:260px, 中:残り全部, 右:320px */
⋮----
/* =========================
   左カラム: フィルタ
========================= */
.col-filter {
⋮----
overflow-y: auto; /* 縦スクロール可 */
⋮----
.filter-panel {
⋮----
.filter-section {
.filter-section:last-child { border-bottom: none; }
⋮----
.filter-title {
⋮----
/* 検索ボックス周り */
.search-box-group {
.input-search-side {
.search-actions {
.btn-icon-side {
.btn-icon-side:hover {
⋮----
/* ラジオボタンリスト */
.radio-group-vertical {
.radio-row {
⋮----
/* ★修正: transition: background 0.1s; */
⋮----
.radio-row:hover { background: #f5f5f5; }
.radio-row.active {
.radio-row input { display: none; }
.radio-icon { margin-right: 8px; font-size: 1.1rem; }
⋮----
/* チェックボックスリスト */
.check-group-vertical {
.check-row {
.check-row input { display: none; }
⋮----
.check-box-ui {
⋮----
/* ★修正: 必要なプロパティのみ指定 */
⋮----
.check-row.checked .check-box-ui {
.check-icon {
⋮----
/* =========================
   中央カラム: 地図
========================= */
.col-map {
⋮----
/* MapPanelの上書き */
.panel--map {
/* 地図上のヘッダーは不要なので消す */
.panel--map .panel-head {
.panel--map .map-area {
⋮----
/* 地図の上に浮く検索ステータスバー */
.map-status-bar {
⋮----
white-space: nowrap; /* スマホで改行されないように */
⋮----
/* =========================
   右カラム: リスト (デザイン刷新)
========================= */
.col-list {
⋮----
background: #f7f9fb; /* 背景を薄いグレーにして、カードを白く浮かせる */
⋮----
/* ListPanelの上書き設定 */
.panel--list {
⋮----
background: transparent !important; /* コンテナ自体は透明に */
⋮----
/* リストヘッダー（固定表示） */
.panel--list .panel-head {
⋮----
flex-shrink: 0; /* スクロール時に縮まないように固定 */
⋮----
.panel--list .panel-title {
⋮----
.panel--list .panel-meta {
⋮----
/* リストエリア（スクロール部分） */
.panel--list .list-area {
⋮----
padding: 12px; /* カード周りの余白 */
⋮----
gap: 12px; /* カード間の隙間 */
⋮----
/* --- トイレカードのデザイン整形 --- */
.toilet-card {
⋮----
border: 1px solid #eee; /* 薄い枠線 */
box-shadow: 0 2px 6px rgba(0,0,0,0.02); /* 優しい影 */
⋮----
/* ★修正: パフォーマンス最適化 (allを使わない) */
⋮----
/* マウスを乗せた時の動き */
.toilet-card:hover {
⋮----
border-color: #bbdefb; /* 青っぽく光らせる */
⋮----
/* --- 上段（名前と距離） --- */
.toilet-header {
⋮----
.toilet-name {
⋮----
margin-bottom: 0; /* 下余白はheaderで管理するためゼロに */
⋮----
/* ★追加: 距離専用のデザイン */
.toilet-distance {
⋮----
white-space: nowrap; /* 折り返し禁止 */
flex-shrink: 0; /* 潰れないようにする */
⋮----
/* --- 下段（アイコン横スクロールエリア） --- */
.toilet-meta {
⋮----
flex-wrap: nowrap; /* ★折り返しを禁止 */
⋮----
overflow-x: auto; /* ★横スクロールを有効化 */
padding-bottom: 4px; /* スクロール時の余白 */
⋮----
/* スクロールバーを隠してスマホアプリっぽくする */
⋮----
.toilet-meta::-webkit-scrollbar {
⋮----
.toilet-meta span {
⋮----
white-space: nowrap; /* 文字の折り返し禁止 */
flex-shrink: 0; /* アイコンが潰れないようにする */
⋮----
/* =========================================================
   追加・修正: フィルタ (アコーディオン・トグルUI)
========================================================= */
⋮----
/* 古い四角いチェックボックスのUIを完全に非表示にする */
⋮----
/* トグルスイッチの行全体のレイアウト */
⋮----
.check-row:last-child {
⋮----
.check-label-content {
⋮----
.check-text {
⋮----
/* ★画像モックアップ風：緑色のトグルスイッチ */
.toggle-switch {
⋮----
.toggle-switch::after {
⋮----
.check-row.checked .toggle-switch {
⋮----
.check-row.checked .toggle-switch::after {
⋮----
.loc-text {
.mobile-toggle-btn {
.accordion-content {
.desktop-only {
⋮----
/* =========================================================
   追加: 検索履歴ポップアップの改善 (PC用ベース)
========================================================= */
.history-popup {
⋮----
top: 48px; /* 入力欄のすぐ下 */
⋮----
/* ヘッダーはPCでは隠す */
.history-popup-header {
⋮----
/* 高さ制限とスクロール */
.history-list {
⋮----
.history-item {
.history-item:hover {
⋮----
.history-footer {
.history-footer:hover {
⋮----
/* 背景はPCでは不要なので隠す */
.history-backdrop {
⋮----
/* =========================================
   ★モバイルレスポンシブ対応 (900px未満)
========================================= */
⋮----
/* ★追加: スマホ時の履歴モーダル化 */
⋮----
background: rgba(0, 0, 0, 0.5); /* 画面全体を暗くする */
⋮----
backdrop-filter: blur(2px); /* 少しぼかす（iOS等で綺麗） */
⋮----
transform: translate(-50%, -50%); /* 画面のど真ん中に配置 */
⋮----
z-index: 2001; /* 背景より上に浮かす */
⋮----
.history-close-btn {
⋮----
max-height: 40vh; /* スマホ画面の40%までの高さでスクロール */
⋮----
/* スマホの時は項目を少し大きくしてタップしやすくする */
⋮----
/* ラッパー：PCでは隠していたスクロールを有効化 */
⋮----
/* 3カラムレイアウト解除 → 縦積みフレックスに変更 */
⋮----
/* スクロール制限を解除し、自然なアコーディオンの挙動に任せる */
⋮----
/* スマホの時は、タイトルを消して画面を広く使う */
⋮----
.desktop-only-btn {
⋮----
.filter-section-top {
⋮----
/* ★画像モックアップ風：検索窓と現在地ボタンの横並び */
⋮----
/* ★現在地ボタンのモックアップ化 */
.btn-location {
⋮----
.loc-icon {
⋮----
/* ★オレンジ色のアコーディオン開閉ボタン */
⋮----
.mobile-toggle-btn:active {
.btn-bottom {
⋮----
/* =========================================
     追加：ボトムシートの登場アニメーション
  ========================================= */
⋮----
/* アコーディオンの中身（滑らかに開閉させる） */
⋮----
/* ★修正: パフォーマンス最適化 (all -> max-height, opacity, padding) */
⋮----
overflow: hidden; /* 閉じている時は中身を隠す */
padding: 0 16px;  /* 縦の余白をなくす */
⋮----
.accordion-content.open {
⋮----
/* ★変更：開いた時の状態 */
max-height: 800px; /* 中身が十分入る高さ */
⋮----
padding: 16px;     /* 余白を復活させる */
⋮----
/* 地図を少し広めに確保（ボトムシートが被さる分を考慮） */
⋮----
border-bottom: none; /* 線を消してシートとの境界を滑らかに */
⋮----
/* ★ここからがボトムシート風 */
⋮----
/* 地図の上に少し被せる（マイナスマージン） */
⋮----
/* 上の角を丸くしてシート感を出す */
⋮----
box-shadow: 0 -4px 16px rgba(0,0,0,0.15); /* 上方向に影を落とす */
padding-top: 12px; /* ハンドル用の隙間 */
⋮----
/* ★追加：読み込み時に下からスッと上がってくるアニメーション */
⋮----
/* シート上部の「つまみ（ハンドル）」を描画 */
.col-list::before {
⋮----
/* リストパネル自体の背景や線を消して馴染ませる */
⋮----
/* =========================================================
   地図上の操作ボタン (徒歩/車, 現在地/地図中心) のデザイン
========================================================= */
.map-controls-overlay {
⋮----
.map-controls-row {
⋮----
.map-ctrl-btn {
⋮----
min-height: 44px; /* スマホでもタップしやすい高さ */
⋮----
/* ★修正: transition: all を廃止 */
⋮----
.map-ctrl-btn:last-child {
⋮----
.map-ctrl-btn:hover:not(:disabled) {
⋮----
.map-ctrl-btn:disabled {
⋮----
/* --- アクティブ（選択中）時の色分け --- */
/* 徒歩（青） */
.map-ctrl-btn.active-walk {
⋮----
/* 車（赤） */
.map-ctrl-btn.active-drive {
⋮----
/* 現在地から / 地図中心から（緑） */
.map-ctrl-btn.active-gps,
</file>

<file path="backend/backend/src/main/java/com/imatoilet/backend/ToiletApiController.java">
public class ToiletApiController {
⋮----
// 検索・一覧取得
⋮----
public ResponseEntity<List<Toilet>> getToilets(
⋮----
List<Toilet> results = toiletService.searchToilets(
⋮----
return ResponseEntity.ok(results);
⋮----
// ID検索
⋮----
public ResponseEntity<Toilet> getToiletById(@PathVariable Long id) {
⋮----
throw new IllegalArgumentException("ID must not be null");
⋮----
return ResponseEntity.ok(toiletService.getToilet(id));
⋮----
// 新規登録
⋮----
public ResponseEntity<Toilet> createToilet(@RequestBody @Valid Toilet toilet) {
Toilet saved = toiletService.createToilet(toilet);
return ResponseEntity.status(HttpStatus.CREATED).body(saved);
⋮----
// 更新
⋮----
public ResponseEntity<Toilet> updateToilet(@PathVariable Long id, @RequestBody @Valid Toilet toiletDetails) {
⋮----
Toilet updated = toiletService.updateToilet(id, toiletDetails);
return ResponseEntity.ok(updated);
⋮----
// 削除
⋮----
public ResponseEntity<Void> deleteToilet(@PathVariable Long id) {
⋮----
toiletService.deleteToilet(id);
return ResponseEntity.noContent().build();
</file>

<file path="backend/backend/src/main/resources/application.properties">
# PostgreSQL への接続設定
# 環境変数 DB_URL, DB_USER, DB_PASSWORD があればそれを使い、なければデフォルト値を使います
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/imatoilet}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:MyNewPass3969}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate の設定
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CORS / Server Config ---
# WebConfig.java の @Value("${app.cors.allowed-origins}") と一致させています
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
app.cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:false}
app.cors.allowed-origins=http://localhost:5173

# ポート設定
server.port=${PORT:8080}

# --- Flyway Config ---
spring.flyway.baseline-on-migrate=true
</file>

<file path="toilet-frontend/src/hooks/useToiletSearch.js">
import { useState, useEffect, useRef } from 'react'; // ★ useRefを追加
⋮----
export const useToiletSearch = () =>
⋮----
// ★追加: 二重実行防止用のガード
⋮----
// ★修正: 初期ロード用 useEffect にガードを追加
⋮----
// 開発モード(StrictMode)での二重実行を防ぐ
⋮----
// eslint-disable-next-line react-hooks/exhaustive-deps
⋮----
// 履歴追加
const addToHistory = (query) =>
⋮----
const handlePlaceSearch = async () =>
⋮----
// API読み込み前のエラーを防ぐ厳格なチェック
⋮----
// Places API (New) の searchByText を使用
⋮----
// 新APIでは必要なフィールドだけを厳密に指定します
⋮----
// 古いコールバック方式から、モダンな async/await (Promise) 方式へ移行
⋮----
// 新APIでは geometry.location ではなく直接 location にアクセスします
⋮----
// name -> displayName, formatted_address -> formattedAddress
⋮----
// 既存ロジック維持：見つからなかった場合はジオコーダー（住所検索）にフォールバック
⋮----
// --- 現在地取得 & 逆ジオコーディング ---
const handleCurrentLocation = () =>
⋮----
// Geocoderが完全に存在するときだけ逆ジオコーディング（住所取得）を行う
⋮----
// APIの読み込みが間に合わなかった場合は、座標だけを使って検索を実行する（エラーで止めない）
⋮----
const handleKeywordSearch = () =>
⋮----
const handleHistorySearch = (query) =>
⋮----
// --- データ取得・フィルタリング ---
⋮----
async function fetchData()
⋮----
// API呼び出し部分 (省略なしでそのまま維持)
⋮----
// (フィルタパラメータの構築)
⋮----
// ... パラメータ追加ロジック ...
⋮----
currentLocation, // 地図の中心
realLocation,    // 本当のGPS現在地
</file>

<file path="toilet-frontend/src/pages/Search/MapPanel.jsx">
// ★修正: Marker のインポートを削除（独自の AdvancedMarker に置き換えるため）
⋮----
// アイコン
⋮----
// --- 既存のSVGピンアイコン生成関数 ---
const getPinIcon = (toilet) =>
⋮----
// =========================================================================
// ★新規追加: AdvancedMarkerElement を安全に呼び出すカスタムコンポーネント
// =========================================================================
const AdvancedMarker = (
⋮----
// onClick関数が再生成されてもピンが再描画されないようRefに保持
⋮----
// 見た目の出し分け（トイレピン、中心ピン、現在地ピン）
⋮----
// =========================================================================
⋮----
function MapPanel(
⋮----
// --- ルート検索ロジック (既存通り維持) ---
⋮----
{/* ★修正: 検索中心のピン */}
⋮----
{/* ★修正: 現在地のピン */}
⋮----
{/* ルート表示 */}
⋮----
{/* ★修正: トイレのピン */}
⋮----
{/* InfoWindow は既存のまま */}
</file>

<file path="toilet-frontend/src/pages/Register.jsx">
// ★Google Maps用コンポーネント
⋮----
// アイコン
⋮----
import DeleteIcon from '@mui/icons-material/Delete'; // ★削除アイコン追加
⋮----
function Register()
⋮----
// --- フォームの状態管理 ---
⋮----
images: [], // ★変更: 複数画像用に配列化
⋮----
// ★Google Maps: 地図をクリックした時の処理
const handleMapClick = (e) =>
⋮----
// ★Google Maps: ピンをドラッグした時の処理
const handleMarkerDragEnd = (e) =>
⋮----
// 3. 入力変更ハンドラ
const handleChange = (e) =>
⋮----
// ★変更: 画像アップロードハンドラ (複数対応)
const handleFileChange = async (e) =>
⋮----
const files = Array.from(e.target.files); // 選択されたファイルを配列化
⋮----
// 並列でアップロードを実行
⋮----
// 既存の画像リストに追加
⋮----
// ★追加: 画像削除ハンドラ
const handleRemoveImage = (indexToRemove) =>
⋮----
const handleStarClick = (rating) =>
⋮----
// 4. 送信処理
const handleSubmit = async (e) =>
⋮----
// ★変更: 画像配列をカンマ区切りの文字列に変換
⋮----
image: imageStr, // ★ここが変更点
⋮----
multiple // ★複数選択を許可
⋮----
{/* ★追加: プレビューリスト（横スクロール or グリッド） */}
</file>

<file path="toilet-frontend/src/pages/Detail.jsx">
// アイコンのインポート
⋮----
// 追加アイコン
⋮----
// ★ライトボックス用アイコン
⋮----
function Detail()
⋮----
// ★ライトボックス用の状態管理 (開いている画像のインデックス。nullなら閉じている)
⋮----
async function fetchToilet()
⋮----
function processToiletData(data)
⋮----
// ★キーボード操作でライトボックスを閉じる/移動する
⋮----
const handleKeyDown = (e) =>
⋮----
}, [lightboxIndex]); // lightboxIndexが変わるたびにイベントリスナーを更新
⋮----
const toggleFavorite = () =>
⋮----
const handleDelete = async () =>
⋮----
// --- ライトボックス操作関数 ---
const openLightbox = (index)
⋮----
const nextImage = (e) =>
⋮----
const prevImage = (e) =>
⋮----
{/* 画像表示エリア */}
⋮----
// 1枚だけの場合：ファーストビューの可能性が高いため lazy は付けず、asyncのみ
⋮----
onClick={() => openLightbox(0)} // クリックで拡大
⋮----
// 複数枚の場合（横スクロール）：画面外の画像は遅延読み込み
⋮----
cursor: 'pointer' // クリックできることを示す
⋮----
onClick={() => openLightbox(idx)} // クリックで拡大
⋮----
{/* ★ここから: ライトボックス(拡大表示)モーダル */}
⋮----
onClick={() => setLightboxIndex(null)} // 背景クリックで閉じる
⋮----
{/* 閉じるボタン */}
⋮----
{/* 前へボタン (画像が2枚以上あるときだけ表示) */}
⋮----
{/* 画像本体：ライトボックスは即表示したいのでlazyなし、ただしasyncは付ける */}
⋮----
onClick={(e) => e.stopPropagation()} // 画像クリックでは閉じない
⋮----
{/* 次へボタン (画像が2枚以上あるときだけ表示) */}
⋮----
{/* 枚数カウント表示 */}
⋮----
{/* ★ここまで */}
</file>

</files>
